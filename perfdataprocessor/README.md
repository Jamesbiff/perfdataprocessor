# EPS Perf Data Processort

## Build

Run the following command in a terminal:

`mvn clean package`

## Use

Run it, passing a path to an input file. Outputs is the same filename with .csv appended.

e.g:

`java -jar .\target\perfdataprocessor-0.1.jar input.txt`
